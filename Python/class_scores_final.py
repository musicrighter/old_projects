class Scores:

	def __init__(self):
		self.projects_scores = []

	def record(self, score):


	if self.projects_scores:
		return sum(self.projects_scores) 
		

	# sum = 0
	# count = 0
	# if len(self.projects_scores) == 0:
	# 	return 0
	# for num in self.projects_scores:
	# 	count += 1
	# 	sum += num
	# return sum / count